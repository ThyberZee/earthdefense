#ifndef UTILS_H
#define UTILS_H

#include <vector>
#include <string>
#include <fstream>

using namespace std;

vector<string> split(string text, char c = ',');

void createLevels();

#endif // UTILS_H
