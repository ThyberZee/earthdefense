#include "worldentity.h"


//Used to access enemy data
void Enemy::getInfo(bool* tempalive, int* tempscorevalue, QString* tempsprite){
    tempalive = alive;
    tempscorevalue = scorevalue;
    tempsprite = sprite;
}

//creates the enemy at set location
void Enemy::create(QString type, QPoint loc){
    if(type = "basic"){

    }
    if(type = "thief"){

    }
}
