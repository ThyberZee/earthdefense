
class InputManager{
    //QObject
    bool left;
    bool right;
    bool fire;

   //getters

   //slots for keydown and keyup.  When keydown, set appropriate bool to trie, when keyup set to false

   //singleton magic
};
